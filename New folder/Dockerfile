# Use the official Nginx image from Docker Hub
FROM nginx:alpine

# Copy your custom Nginx configuration
COPY nginx.conf /etc/nginx/nginx.conf

# Copy your website's static files
COPY index.html /usr/share/nginx/html/
